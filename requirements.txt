# Root requirements (one-shot install for this workspace)
#
# This file intentionally merges:
# - agentbeats-tutorial/pyproject.toml runtime deps (A2A runner + scenario servers)
# - defi_agent_eval/requirements.txt deps (DeFi + trading evaluator)
# - trading backtest extras used by the trading scenario (pandas, numpy)
#
# Note: we keep versions conservative. If you prefer exact locking, use uv.lock / pyproject workflows.

# --- AgentBeats tutorial runner / A2A ---
a2a-sdk>=0.3.5
uvicorn>=0.35.0
python-dotenv>=1.1.1
pydantic>=2.11.9
loguru>=0.7.0

# LLM client helper (used by some scripts/agents)
litellm>=1.0.0

# Optional (tutorial debate scenario)
google-adk>=1.14.1
google-genai>=1.36.0

# --- Trading evaluator (data + indicators) ---
requests>=2.31.0
pandas>=2.2.0
numpy>=1.26.0

# --- DeFi evaluator dependencies (kept compatible with existing pins) ---
web3==6.11.3
eth-account==0.10.0
eth-typing==3.5.2
eth-utils==2.3.1
py-solc-x==2.0.2

# These are only needed if you run the original DeFi white agents:
openai==1.3.0
anthropic==0.7.0

# --- Dev / tests (optional) ---
pytest>=7.4.3
black>=23.11.0

